# LLM and Societal Consequences of AI 

## Installation
The code runs on Python (3.12), there are three ways you can proceed with the installation:
1. Install docker and use a containerized setup.
2. Install Python with mamba/conda and install the provided requirement.txt (or similar) files.
3. Install Python and install the required packages manually (pip).
